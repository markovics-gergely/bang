import { CharacterTypePipe } from './character-type.pipe';

describe('CharacterTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CharacterTypePipe();
    expect(pipe).toBeTruthy();
  });
});
