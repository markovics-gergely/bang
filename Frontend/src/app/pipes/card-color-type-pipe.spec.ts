import { CardColorTypePipe } from './card-color-type.pipe';

describe('CardColorTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CardColorTypePipe();
    expect(pipe).toBeTruthy();
  });
});
